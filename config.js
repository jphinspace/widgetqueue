// Widget Queue Simulation - Configuration
// Simulation configuration constants

const CONFIG = {
    canvasWidth: 800,
    canvasHeight: 600,
    counterHeight: 60,
    customerRadius: 15,
    baseSpeed: 50, // pixels per second
    decisionInterval: 3000, // ms between decisions
    queueSpacing: 35,
    counterWidth: 200,
    counterY: 40,
    exitWidth: 100,
    exitHeight: 40,
    entranceWidth: 100,
    entranceHeight: 40,
    purchaseTime: 2000 // ms to complete purchase
};
